spring:
  datasource:
    url: jdbc:mysql://localhost:3306/study?useUnicode=true&characterEncoding=UTF-8&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: admin
    driver-class-name: com.mysql.jdbc.Driver
server:
  port: 22222
mybatis:
  mapper-locations: classpath:config/mybatis/mapper/mapper_*.xml
  type-aliases-package: cloud.entity
logging:
  config: classpath:config/log/log.xml
  level:
    root: debug

