spring:
  profiles:
    active: dev
server:
  servlet:
    context-path: /Spring-Cloud-Eureka
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:22224/eureka/